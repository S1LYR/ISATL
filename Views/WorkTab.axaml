<UserControl xmlns="https://github.com/avaloniaui"
			 xmlns:vm="clr-namespace:IAFTS.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="IAFTS.Views.WorkTab"
			 x:DataType="vm:MainWindowViewModel">
	<Grid ColumnDefinitions="810,30,*" Margin="16">
		<!-- Левая часть -->
		<StackPanel Grid.Column="0" Spacing="8">
			<Border Background="#CAF3CF" CornerRadius="8" Padding="8">
				<StackPanel>
					<TextBlock Text="🔍 Окно отображения" FontSize="18" VerticalAlignment="Center"/>
					<Border Height="400" Background="White" CornerRadius="6" Margin="0,8,0,0">
						<Image Stretch="Uniform" Height="800" Source="{Binding TreeDetectionViewModel.Image}" >
							<Image.RenderTransform>
								<RotateTransform Angle="90" CenterX="0" CenterY="0"/>
							</Image.RenderTransform>
						</Image>
					</Border>
				</StackPanel>
			</Border>
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="8">
				<Button Content="+ Файл .tiff" Command="{Binding TreeDetectionViewModel.LoadTiffCommand}"
						Background="#2DB874" Foreground="White" CornerRadius="20"/>
				<Button Content="+ Файл .las" Command="{Binding TreeDetectionViewModel.LoadLasCommand}"
						Background="#2DB874" Foreground="White" CornerRadius="20"/>
			</StackPanel>
		</StackPanel>
		<!-- Правая часть -->
		<StackPanel Grid.Column="2" Spacing="0">
			<Border Background="#CAF3CF" CornerRadius="8" Padding="8">
				<StackPanel>
					<TextBlock Text="🔍 Вывод из отчета" FontSize="18" VerticalAlignment="Center"/>
					<!-- Здесь будет содержимое окна -->
					<Border Height="400" Background="White" CornerRadius="6" Margin="0,8,0,0">
						<DataGrid ItemsSource="{Binding TreeDetectionViewModel.Data}"
							  AutoGenerateColumns="False"
							  IsReadOnly="True"
							  CanUserReorderColumns="False"
							  CanUserResizeColumns="False" >
							<DataGrid.Columns>
								<DataGridTextColumn FontSize="10" Width="150" Header="x" Binding="{Binding x, StringFormat={}{0:F2}}" />
								<DataGridTextColumn FontSize="10" Width="150" Header="y" Binding="{Binding y, StringFormat={}{0:F2}}" />
								<DataGridTextColumn FontSize="10" Width="150" Header="h" Binding="{Binding h, StringFormat={}{0:F2}}" />
								<DataGridTextColumn FontSize="10" Width="150" Header="s" Binding="{Binding s, StringFormat={}{0:F2}}" />
							</DataGrid.Columns>
						</DataGrid>
					</Border>
				</StackPanel>
			</Border>
			<Grid Margin="0,8,0,0">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>
				<Button Content="Расчет объема" Background="#2DB874"
						Foreground="White" CornerRadius="20"
						HorizontalAlignment="Left"
						Command="{Binding TreeDetectionViewModel.ProcessDataCommand}"/>
				<Button Content="Сохранить отчет" Grid.Column="1" Background="#2DB874"
						Foreground="White" CornerRadius="20"
						HorizontalAlignment="Right"
						Command="{Binding TreeDetectionViewModel.SaveResultsCommand}"/>
			</Grid>
		</StackPanel>
	</Grid>
</UserControl>

